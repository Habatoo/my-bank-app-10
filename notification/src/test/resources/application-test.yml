server:
  port: 0

spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    compatibility-verifier: false
    config:
      enabled: false
  liquibase:
    enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          enabled: false