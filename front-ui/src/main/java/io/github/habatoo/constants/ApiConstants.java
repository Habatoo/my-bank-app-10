package io.github.habatoo.constants;

/**
 * Класс, содержащий базовые константы для сетевого взаимодействия приложения.
 * <p>
 * Данный класс служит централизованным хранилищем глобальных параметров,
 * таких как базовые URL микросервисов, пути к API и конфигурационные настройки шлюза.
 * </p>
 * <p>
 * Класс является финальным и имеет приватный конструктор для предотвращения создания экземпляров.
 * </p>
 */
public final class ApiConstants {

    /**
     * Базовый URL сетевого шлюза (Gateway) для обращения к микросервисам через API.
     * Используется внутри внутренней сети Docker для маршрутизации запросов.
     */
    public static final String BASE_GATEWAY_URL = "http://gateway:8080";

    /**
     * Приватный конструктор для запрета инициализации утилитарного класса.
     */
    private ApiConstants() {
        throw new UnsupportedOperationException("Это утилитарный класс и он не может быть инициализирован");
    }
}
